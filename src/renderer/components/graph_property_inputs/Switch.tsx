import React, { Component } from "react";
import { Switch as Sw } from "@blueprintjs/core";
export default class Switch extends Component {
  render() {
    return (
      <div>
        <Sw className="gcp-switch" />
      </div>
    );
  }
}
